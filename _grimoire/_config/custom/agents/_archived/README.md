# Archive legacy custom agents (grimoire-kit)

Déplacés ici **21 avril 2026** suite à la review E2E. Voir le README.md du meta-repo pour le détail des remplaçants modernes (mêmes fichiers, même analyse).

Canon : `_grimoire/` est P5 archéologie uniquement. Ces agents héritent de l'ancien `_bmad/_config/custom/agents/` et ne sont référencés par aucun workflow, skill ou manifest actif dans `_grimoire-runtime/`, `framework/`, `src/grimoire/` ou les manifests du kit.

Le scanner `harmony_check` exclut automatiquement les dossiers préfixés `_archive*`.
